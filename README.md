# open-bottlenose-framework
Local persistence boundary for LLM agents. Provides overlay-reader (read-only) and overlay-writer (controlled write) services so agents never access the database directly. Designed for OpenClaw and open-bottlenose memory systems.
